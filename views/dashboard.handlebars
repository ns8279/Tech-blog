
<section class = "mx-5 my-5">
<h2> Create New Blog</h2>

        <form class ="new-post-form">
        <div class="form-group ">
            <label for="post-title">Title</label>
            <input type="text" class="form-control" id="post-title" name = "post-title" />
            
        </div>
        <div class="form-group">
            <label for="post-text">Text</label>
            <textarea class = "form-control" id =" post-text" rows = "3" name ="blog-body"></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Create</button>
        </form>
</section>

{{#if posts.length}}
<section class = "mx-5 my-5">
    <h2> Your Blogs </h2>
    {{#each posts as |post|}}
    <div class="card text-white bg-dark mb-3 mx-5 my-5 rounded">
    <h5 class="card-header text-white"> {{post.title}} <span class="float-right">{{format_date post.created_at}}</span></h5>
    <div class="card-body">
        <p class="card-text text-white">{{post.post_text}}</p>
        <a href="/post/{{post.id}}" class="btn btn-primary">{{comments.length}} {{format_plural "comment" post.comments.length}}</a>
        <a href ="/dashboard/edit/{{post.id}}" class ="btn btn-primary edit-link">Edit Blog</a>
    </div>
    </div>
    {{/each}}
</section>
{{/if}}


<script src = "/javascript/add-blog.js"></script>